import React, { Component } from 'react'

export default class AccountInfo extends Component {
    render() {
        return (
            <div>
                We're going to call this from another page and re render it
            </div>
        )
    }
}
