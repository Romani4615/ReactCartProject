import React, { Component } from 'react'

export default class ShopProducts extends Component {
    render() {
        return (
            <div>
                Shop Page, products go here
            </div>
        )
    }
}
